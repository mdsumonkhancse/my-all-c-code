#include <stdio.h>

int main()
{
    int price; // interger type variable declartion
    int marks;
    float temparature; // float typs variable declartion

    price = 20; // variable assign
    printf("%d\n", price);

    price = 30;
    printf("%d\n", price);

    // or
    price = 20 + 30;
    printf("%d\n", price);

    // or
    marks = 90;
    printf("%d\n", marks);

    // or

    price = marks - 10;
    printf("%d\n", price);

    // eki line a variable assign and declare
    int price1 = 90 + 30;
    printf("%d\n", price1);

    return 0;
}